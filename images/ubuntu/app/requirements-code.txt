# 轻量 code 沙箱（无 GUI）的 Python 依赖
# = 基础运行依赖（terminal/file/system/process 路由）+ 日常办公/数据处理常用库
#   + playwright（headless chromium 随镜像预装，供 agent 脚本做网页抓取/自动化；
#     code profile 仍不加载 browser 路由，GUI 桌面栈只在 ubuntu 镜像）。
# 业务可在运行时通过 pip install 追加（已配置清华 pip 镜像）。

-r requirements-base.txt

# ── 表格 / 数据 ──────────────────────────────────────────────
openpyxl>=3.1.0
XlsxWriter>=3.1.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.11.0
tabulate>=0.9.0

# ── 文档（Word / PPT / PDF）─────────────────────────────────
python-docx>=1.1.0
python-pptx>=0.6.23
reportlab>=4.0.0
pypdf>=4.0.0
pdfplumber>=0.11.0
PyMuPDF>=1.24.0                  # import fitz：agent 读 PDF 的第一选择（提文字 / 渲染页面图）
# 原来只装 [pptx]，MarkItDown().convert('x.docx') 当场报缺 mammoth（B 端失败日志）
markitdown[docx,xlsx,xls,pdf,pptx,outlook]>=0.1.0

# ── 旧版 Office / WPS 二进制格式（.doc .xls .wps .msg）─────────
# B 端客户断网，现场 pip 装不了；失败日志里 olefile 缺失 10 次、xlrd 4 次，
# agent 只能手写 OLE 解析，一份 .doc 耗掉十几个回合。配套系统命令见 Dockerfile（antiword/catdoc/file）。
olefile>=0.47                    # OLE2 复合文档容器，.doc/.xls/.wps/.msg 解析基础
xlrd>=2.0.1                      # 读 .xls（pandas.read_excel 的 xls 引擎）
msoffcrypto-tool>=5.4.0          # 解带密码的 Office 文件

# ── 图像 / 图表 ──────────────────────────────────────────────
Pillow>=10.0.0
matplotlib>=3.7.0
seaborn>=0.13.0

# ── 浏览器自动化（headless，Dockerfile 里随镜像预装 chromium）─
playwright>=1.45.0

# ── 文本 / 网页 / 网络 ───────────────────────────────────────
lxml>=5.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0
httpx>=0.27.0
PyYAML>=6.0
charset-normalizer>=3.3.0
